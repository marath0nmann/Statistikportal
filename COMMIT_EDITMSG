v1080 – Fix: Admin → System – Neuester Benutzer wurde nie angezeigt; Ursache: SQL-Query selektierte nicht-existente Spalten `vorname`/`nachname` → stiller try/catch-Fehler → immer null; Query auf vorhandene Spalten `benutzername`/`email` korrigiert
