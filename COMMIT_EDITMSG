v409: Fix diszMitKat + Timeline: kein falscher Kategorie-Suffix

- diszMitKat(): bei mehrdeutigem Name (z.B. 800m in 3 Kategorien)
  kein Suffix anzeigen statt falschen; mit mapping_id exakt
- Timeline diszLink: ergDiszLabel(rek) statt diszMitKat(rek.disziplin)
  → nutzt kategorie_name direkt aus API-Response
