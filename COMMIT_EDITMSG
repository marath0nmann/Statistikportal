v508: RaceResult-Proxy: robustere Event-ID-Erkennung

5 Suchmuster statt nur RRPublish-Inline:
1. RRPublish(element, ID, ...) inline
2. raceresult.com/ID/ URL-Muster im HTML
3. data-event/data-rrid Attribute
4. Script-src mit raceresult.com/ID/
5. Numerisches Argument in Publish-Funktionen in Inline-Scripts
API gibt event_id direkt zurück (kein HTML-Transfer mehr)
