v537: Fix TOTP-Deaktivierung + loginStep2 NULL-Secret

- loginStep2: totp_secret=NULL → klar Fehler statt 500
- DELETE totp-setup: migrate() vor userHasPasskey() (Tabelle muss existieren)
- disableTotp(): Vorprüfung im Frontend, 409 klar erklären
