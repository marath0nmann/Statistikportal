v275: RaceResult Metadaten + Name-Matching

- eventName: cfg.eventname als Fallback
- Datum-Fallback "heute" entfernt (Feld bleibt leer wenn unbekannt)
- Ort: aus letztem Wort der HeadLine extrahiert
- Name-Matching: SS↔ß, Umlaute (ä→ae etc.), Komma/Bindestrich ignoriert
  → "HEISS Simon" matcht jetzt "Heiß, Simon"
