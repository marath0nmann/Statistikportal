v219: Eintragen direkt Bulk + RaceResult Club-Filter Fix

- navigate(): Eintragen-Tab öffnet jetzt direkt Bulk-Eintragen (subTab='bulk')
- RaceResult: &term= (Namenssuche) entfernt - lud falsche/keine Ergebnisse
  Stattdessen: alle Ergebnisse laden (&r=all), dann clientseitig nach
  Club-Spalte auf Vereinsname filtern
