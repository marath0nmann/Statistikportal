v477: Fix PUT disziplin_mapping_id

API-PUT nutzte immer ersten DB-Treffer für "800m" → immer Straße.
Wenn disziplin_mapping_id direkt im Body mitgeschickt wird (aus v476),
wird diese direkt gesetzt. Auch mapping_id-only-Änderung möglich.
