v535: TOTP-Deaktivierung: korrekte Prüfung + Warnung

- API: TOTP-Deaktivierung nur erlaubt wenn Passkey vorhanden (sonst 409)
- Frontend: Warntext je nach Passkey-Status ("weiterhin per Passkey" vs "Zugang verloren")
- notify() statt alert()
