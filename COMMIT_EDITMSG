v510: RaceResult-Proxy: DOM-Ansatz entfernt (war fehlerhaft)

Performance-Entries und DOM-Suche entfernt — lasen alte Requests aus dem
aktuellen Tab (statistik.tus-oedt.de) statt der Zielseite.

Jetzt: nur sauberer Server-Proxy. Bei dynamisch geladenen Seiten (JS-Rendering)
klare Fehlermeldung mit Anweisung zum Netzwerk-Tab.
