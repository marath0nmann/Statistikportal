v250: RaceResult: r=all als Fallback wenn r=search keine Daten liefert

Manche RaceResult-Events unterstützen keinen Search-Endpoint →
0 Zeilen trotz gültiger Antwort. Neues Verhalten:
1. Erst r=search versuchen
2. Wenn 0 Zeilen → r=all als Fallback
Debug zeigt jetzt "[r=all]" wenn Fallback aktiv war
