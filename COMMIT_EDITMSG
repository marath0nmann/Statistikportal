v556: RaceResult Import: normalizeAK() auch im Import-Loop

normalizeAK() wurde nur beim Anzeigen der Tabelle aufgerufen, nicht
beim tatsächlichen Import → "Seniorinnen W40" kam roh in isValidDlvAK()
→ unbekannte AK Dialog erschien obwohl die AK korrekt angezeigt wurde.
