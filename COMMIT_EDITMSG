v564: Fix uitslagen.nl SubTab: content vor innerHTML gesetzt

isUits-Block setzte content NACH dem innerHTML-Aufruf →
Seite war leer. Block vor document.getElementById('main-content').innerHTML verschoben.
