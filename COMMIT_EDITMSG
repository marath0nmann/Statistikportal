v482: Fix RaceResult iNetto=iClub-Kollision (Ziel.GUN-Event)

Default iNetto=7 kollidierte mit iClub=7 wenn kein Chip/Netto-Feld.
Fix:
- iNetto und iZeit Default auf -1 (beide Blöcke)
- Fallback: iNetto = iZeit wenn kein Netto-Feld erkannt
- Sicherheits-Check: iNetto !== iClub
