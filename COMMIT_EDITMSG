v524: RaceResult: Fix Datenstruktur-Erkennung (Row direkt vs Array von Arrays)

Martins-Crosslauf: data = {"0": [row], "1": [row], ...} — jeder Value
ist direkt eine Row, kein Array von Rows. flat() zerlegte Strings in Zeichen.

Fix: wenn v[0] kein Array → v ist selbst eine Row → groups = {"": [v]}
Betrifft alle 3 Fetch-Blöcke. TuS Oedt (3 Treffer) wird jetzt gefunden.
