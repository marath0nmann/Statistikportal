v542: Fix Login-Response: has_totp + has_passkey wurden nicht weitergegeben

api/index.php: jsonOk() im Login-Endpunkt enthielt nur totp_required/totp_setup,
hat has_totp und has_passkey aus loginStep1 ignoriert → Frontend sah nie
has_passkey:true → zeigte immer TOTP statt Passkey-Dialog.
