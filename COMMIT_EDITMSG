v536: Passkey-Robustheit: Stub-Fallback + migrate() + try-catch

- auth.php: passkey.php optional (Stub-Klasse wenn Datei fehlt → kein Fatal)
- loginStep1: Passkey::migrate() vor userHasPasskey() (Tabelle anlegen)
- auth/me: migrate() ebenfalls
- userHasPasskey(): try-catch (DB-Fehler → false, kein Absturz)
