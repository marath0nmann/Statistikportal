v239: RaceResult Kategorie-Pflichtfeld vollständig verdrahtet

- Kategorie-Dropdown bereits vorhanden, aber Verdrahtung unvollständig:
  - rrKatChanged() prüft jetzt URL + Kategorie (beide Pflicht für Button-Freigabe)
  - URL-Feld: oninput="rrKatChanged()" damit Button auch bei URL-Eingabe reagiert
  - _rrState speichert rrKatKey für Import
  - diszList in rrRenderPreview filtert auf gewählte Kategorie (war schon via _kat)
