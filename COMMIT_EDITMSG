v340: Syntax-Fixes (Quote-Escaping in HTML-Attributen)

- avatarHtml: onerror-Attribut mit HTML-Entities statt escaped Quotes
- Header-Avatar: onerror entfernt (kein verschachtelter Quote-Konflikt)
- Hall of Fame Fallback-Avatar: font-family ohne Quotes in Inline-Style
- node --check meldet jetzt keine Warnings mehr
