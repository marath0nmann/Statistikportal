v565: uitslagen.nl: alle Kategorie-Container iterieren

Seite hat 30 .uitslagen-Divs (je einer pro Kategorie), nicht einen.
Parser iterierte nur den ersten (23 Zeilen statt 527).
Fix: querySelectorAll('.uitslagen') + äußeres forEach über alle Container.
