v363: Hall of Fame: Kategorie-Filter + Bug-Fix sort_dir

Bug: sort_dir wurde per Regex ermittelt statt aus disziplin_mapping →
bei manchen Disziplinen falsche Richtung → falsche Bestleistung.
Fix: sort_dir aus COALESCE(k.sort_dir, 'ASC') im JOIN.

Neu: Kategorie-Checkboxen in der Widget-Konfiguration (Standard: alle).
API: ?kat=strasse,sprint (kommagetrennt).
