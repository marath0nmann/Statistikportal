v277: Datum als Textfeld + erweiterter Debug

- Datum: type=text statt type=date → bleibt wirklich leer wenn kein Datum von RaceResult
- Format TT.MM.JJJJ, wird beim Import zu YYYY-MM-DD konvertiert
- Debug stark erweitert: DataFields, alle Spaltenindizes, eventName/Date/Ort,
  cfg-Rohdaten, alle Contest-Namen, diszList, rrBestDisz-Ergebnis, clubSamples
