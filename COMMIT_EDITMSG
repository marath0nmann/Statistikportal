v246: pace nicht mehr speichern, aus DB löschen

- JS: pace wird nicht mehr an die API gesendet (Einzeleintragen, Bulk, RR-Import)
- JS: Alle Anzeige-Stellen nutzen nur noch calcPace() on-the-fly
- API: pace aus allen INSERT/UPDATE-Statements entfernt
- Migration: migration_pace_remove.sql leert bestehende pace-Werte in beiden Tabellen
  (Spalte bleibt für Schema-Kompatibilität, wird nicht mehr befüllt)
