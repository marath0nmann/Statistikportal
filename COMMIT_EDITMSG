v1072 – Feature: TOTP-Setup – kopierbarer Secret-String mit „📋 Kopieren"-Button neben dem QR-Code (Registrierung, Admin-Setup, Profil-Modal); Hilfsfunktionen `_totpSecretHtml()` und `_copyTotpSecret()` zentralisieren die Darstellung
