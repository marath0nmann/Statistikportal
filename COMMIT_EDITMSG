v331: Dashboard: responsive Layout für Smartphones

.dash-row mit repeat(auto-fit, minmax(var(--dash-min), 1fr)):
Spalten umbrechen automatisch wenn der Platz nicht reicht,
ohne feste Breakpoints – funktioniert für alle Bildschirmbreiten.
