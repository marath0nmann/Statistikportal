v579: Import-Debug erweitert (Fix weißer Bildschirm)

v578 hatte Syntax-Fehler durch ' in Regex /^[0-9:.,'-]+$/ und 
fehlerhafte Klammern in den neuen Debug-Helfer-Strings.

Neuansatz: 07_eintragen.js auf v577 zurückgesetzt, dann sauber
per str_replace und line-by-line Ersetzung erweitert.

Debug-Log enthält jetzt:
- Header: Version, Verein, Zeitstempel, URL, Quelle, Kategorie
- Pro Quelle: Event-Details + Ergebnisliste mit Name/AK/Zeit/Platz/Disziplin
