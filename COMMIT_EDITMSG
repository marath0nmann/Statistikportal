v273: RaceResult: Fallback auf echte Contest-IDs wenn contest=0 hängt

contest=0 + r=all hängt bei diesem Event (kein Response).
Neue Strategie:
- contest=0 mit 5s Timeout probieren
- Bei Timeout: alle echten IDs aus cfg.contests einzeln versuchen
- Echte Contests mit 15s Timeout
