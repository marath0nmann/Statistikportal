v460: RaceResult: StatusPlatz([AKPl.P]) + Disziplin aus groupKey

- StatusPlatz([AKPl.P]) → iAKPlatz erkannt (war nur MitStatus/AutoRankP)
- Disziplin: groupKey "#2_Westenergie Marathon/#4_Weiblich" → "Westenergie Marathon" → Marathon
