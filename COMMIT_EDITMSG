v251: RaceResult Debug: topKeys immer setzen + r=all Raw anzeigen

- topKeys wurde nie gesetzt wenn DataFields vorhanden waren → immer "?"
- r=all Fallback zeigt jetzt den rohen Response im Debug
- payloadHasRows() Hilfsfunktion vereinheitlicht die Daten-Prüfung
- fetchMode zeigt jetzt auch "r=all (leer)" wenn beide Endpoints leer sind
