v559: Rekorde by_ak: e.disziplin=? → $diszCond/$diszParam

by_ak-Query filterte per Name statt per mapping_id → alle gleichnamigen
Disziplinen (Bahn + Cross + Halle) wurden gemischt in den AK-Bestleistungen.
Fix: $diszCond + $diszParam konsistent wie gesamt/maenner/frauen.
