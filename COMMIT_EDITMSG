v408: Fix Bestleistungen/Rekorde: Disziplin per mapping_id statt Name

- Bestleistungen (Timeline): diszMap nach mapping_id gruppiert
  → 800m Bahn, 800m Straße, 800m Halle sind 3 separate Einträge
- Rekorde top-disziplinen + disziplinen: COUNT per mapping_id
- Rekorde-Queries: WHERE disziplin_mapping_id=? statt disziplin=?
- Frontend: mapping_id in setRekDisz, API-Call, Disziplin-Buttons
