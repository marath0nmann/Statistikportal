v271: RaceResult: r=all primär, term= entfernt, Timeouts

- term=tus+oedt hängt RaceResult-Server → entfernt
- r=all immer als Daten-Request (nicht nur Fallback)
- 8s Timeout auf r=search, 10s auf r=all und Extra-Requests
- Client-seitiger Club-Filter bleibt wie gehabt
