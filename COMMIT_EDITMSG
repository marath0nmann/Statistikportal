v452: Fix MikaTiming: li-Regex + event= aus Hauptseite

- li class=" list-active event-M_2EF3BRLP2..." (führendes Leerzeichen) → Regex fix
- idp nur wenn >= 8 Zeichen (filtert "start" heraus)
- event= aus Hauptseite wenn nicht in URL → idp-Links werden korrekt gesetzt
